# Per-instance sandbox state (generated at runtime)
sandboxes/*/
.state/

# Secrets — never commit
.sandbox.env
*.htpasswd
sandbox-ai.toml

# Legacy / deprecated
.deprecated/
temp/

# Maintainer-internal internal-tooling state — backlog, ongoing explorations,
# validation records, findings, transcripts (not shipped to the public repo)
openspec/explorations/

# Tooling
.antigravityignore
.agent/
.venv/
.claude/
.claudeignore

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

dist/
scripts/integration-probe.sh
docs/assets/
_bmad-output/
_bmad/
