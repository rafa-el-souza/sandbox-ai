name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          
      - name: Set up Python
        run: uv python install
        
      - name: Install dependencies
        run: uv sync --all-extras --dev
        
      - name: Run Ruff
        run: uv run ruff check core/ cli/ tests/
        
      - name: Run Mypy
        run: uv run mypy core/ cli/ tests/
        
      - name: Run Pytest
        run: uv run pytest --cov=core --cov=cli --cov-fail-under=98 tests/unit/
